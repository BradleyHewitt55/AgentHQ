# Project tasks

Tasks belong to a project and appear in that project's **Tasks** right-panel surface. Select one or
more task checkboxes, then choose **Pass to agent** to add a combined handoff prompt to the current
project thread. Passed tasks move to **In progress** and keep their linked GitHub issues in sync when
GitHub linking is enabled.
